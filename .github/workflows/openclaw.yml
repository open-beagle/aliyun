name: openclaw

on:
  push:
    branches:
      - openclaw

env:
  VERSION: 2026.3.13

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Aliyun Container Registry
        uses: docker/login-action@v2
        with:
          registry: registry.cn-qingdao.aliyuncs.com
          username: ${{ secrets.REGISTRY_USER_ALIYUN }}
          password: ${{ secrets.REGISTRY_PASSWORD_ALIYUN }}

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: ./openclaw
          file: ./openclaw/dockerfile
          platforms: linux/amd64
          push: true
          provenance: false
          tags: |
            registry.cn-qingdao.aliyuncs.com/wod/openclaw:${{ env.VERSION }}
            ghcr.io/${{ github.repository_owner }}/openclaw:${{ env.VERSION }}
          build-args: |
            BASE=justlikemaki/openclaw-docker-cn-im:2026.3.11
            AUTHOR=mengkzhaoyun@gmail.com
            VERSION=${{ env.VERSION }}

      - name: Image build success
        run: echo "openclaw:${{ env.VERSION }} built and pushed successfully!"
